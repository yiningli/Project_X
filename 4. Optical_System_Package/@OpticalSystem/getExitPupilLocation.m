function ExitPupilLocation = getExitPupilLocation(optSystem,wavLen)
    % getExitPupilLocation: returns EXP location from image surf as
    % in zemax
    [ ExitPupilLocation ] = computeExitPupilLocation...
        (optSystem);
end