function BFL = getBackFocalLength(optSystem,wavLen)
    % getBackFocalLength: returns locatio of paraxial focus after
    % last surface
    
    BFL  = computeBackFocalLength(optSystem);
    
end