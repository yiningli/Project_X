function angMag = getAngularMagnification(optSystem,wavLen)
    % getParaxialMagnification: returns paraxial magnification of
    % the system
    angMag = computeParaxialAngularMagnification(optSystem);
end