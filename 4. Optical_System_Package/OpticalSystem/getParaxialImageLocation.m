function paraxialImageLocation = getParaxialImageLocation(optSystem,wavLen)
    % getEffectiveFocalLength: returns location of paraxial focus
    % from the principal plane
    paraxialImageLocation = computeParaxialImageLocation(optSystem);
end