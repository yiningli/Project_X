function [ componentDescription ] = getComponentDescription( currentComponent )

componentDefinitionFileName = currentComponent.Type;
% Connect the component definition function
componentDefinitionHandle = str2func(componentDefinitionFileName);
returnFlag = 8;
[ exitRayPosition] = surfaceDefinitionHandle(...
    returnFlag,surfaceParameters,rayPosition,rayDirection,...

end

